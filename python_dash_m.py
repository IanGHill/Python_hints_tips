# in the terminal if you run a module with a -m e.g. puthon3 -m my_module.py
# -m will search sys.path for the named module and execute its contents as the main module
# i.e. -m can be used to run modules not in the current directory (also don't use .py extension
# so you can try python3 -m ternaries